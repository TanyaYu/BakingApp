<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable name="context" type="com.example.tanyayuferova.bakingapp.ui.MainActivity"/>
        <variable name="recipe" type="com.example.tanyayuferova.bakingapp.entity.Recipe" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:onClick="@{() -> context.recipeItemOnClick(recipe)}">

        <ImageView
            android:id="@+id/iv_recipe_preview"
            android:layout_width="match_parent"
            android:layout_height="180dp"
            app:imageUrl="@{recipe.image}"
            app:error="@{@drawable/ic_cupcake_color}"
            tools:src="@drawable/ic_cupcake_color" />

        <TextView
            android:id="@+id/tv_recipe_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@{recipe.name}"
            tools:text="Recipe name" />

    </LinearLayout>
</layout>